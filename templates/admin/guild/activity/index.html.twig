{% extends 'admin.html.twig' %}

{% block body %}

  <h1>Activités <a href="{{ path('admin_guilds_activities_edit', {'id': 'new'}) }}" class="btn btn-outline-primary"><i class="fas fa-plus"></i></a></h1>

  <table class="table table-hover">
    <thead>
      <tr>
        <th>UID</th>
        <th>FR</th>
        <th>EN</th>
        <th>DE</th>
        <th>ES</th>
        <th>&nbsp;</th>
      </tr>
    </thead>
    <tbody>
      {% for activity in activities %}
        <tr>
          <td>{{activity.uid}}</td>
          <td>{{activity.fr}}</td>
          <td>{{activity.en}}</td>
          <td>{{activity.de}}</td>
          <td>{{activity.de}}</td>
          <td><a href="{{ path('admin_guilds_activities_edit', {'id': activity.id}) }}"><i class="fas fa-edit"></i> Modifier</a></td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

  <div class="content">

{% endblock %}
